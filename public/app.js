(() => {
  const $ = id => document.getElementById(id);
  const canvas = $('plot'), ctx = canvas.getContext('2d');
  const NOTE = ['C','C#','D','D#','E','F','F#','G','G#','A','A#','B'];
  let drag = null, audioCtx = null, source = null, raf = 0, playStart = 0;

  const vm = new Vue({
    el: '#app',
    data: { status:'等待音频…', meta:'', data:null, fileBuffer:null, noteLines:false, sampled:true, beatLines:false, beatNotes:true, dismiss:false, bpm:120, meter:'4/4', track:'max', fromStart:true, playing:false, playPos:0, notes:[], view:{x0:0,x1:1,y0:0,y1:22050} },
    watch: {
      bpm(){if(this.data){this.makeNotes();this.draw();}}, meter(){this.draw();},
      noteLines(){this.draw();}, sampled(){this.draw();}, beatLines(){this.draw();}, beatNotes(){this.draw();}, dismiss(){this.draw();}, track(){this.draw();}
    },
    methods: {
      resize(){const d=devicePixelRatio||1;canvas.width=Math.max(1,canvas.clientWidth*d);canvas.height=Math.max(1,canvas.clientHeight*d);ctx.setTransform(d,0,0,d,0,0);this.draw();},
      reset(){if(this.data)this.view={x0:0,x1:this.data.duration,y0:0,y1:this.data.fMax};this.draw();},
      makeNotes(){const bd=60/Math.max(1,Number(this.bpm)),count=Math.ceil(this.data.duration/bd);this.notes=[];for(let i=0;i<count;i++){const t=i*bd,arr=[];for(const [tt,cands] of this.data.tonesTrack)if(tt>=t&&tt<t+bd)for(const c of cands){const old=arr.find(x=>x[0]===c[0]);old?old[2]+=c[2]:arr.push([...c]);}arr.sort((a,b)=>b[2]-a[2]);const c=arr[0]||['—',0,0];this.notes.push({id:i,t,name:c[0],freq:c[1],candidates:arr.slice(0,15),full:false,dismiss:false});}},
      async loadFile(file){this.status='读取 WAV…';this.fileBuffer=await file.arrayBuffer();try{this.status='分析中…';await new Promise(requestAnimationFrame);this.data=analyzeWav(new Uint8Array(this.fileBuffer),2048,512);this.view={x0:0,x1:this.data.duration,y0:0,y1:this.data.fMax};this.makeNotes();this.meta=`${file.name} · ${this.data.duration.toFixed(3)}s · ${this.data.sampleRate}Hz`;this.status='分析完成';this.resize();}catch(e){this.data=null;this.status=e.message||'WAV 分析失败';}},
      draw(){drawCanvas(this);},
      async togglePlay(){if(!this.data)return;if(this.playing){stopPlayback(this);return;}await startPlayback(this,this.fromStart?0:this.playPos);}
    },
    mounted(){this.resize();window.addEventListener('resize',this.resize);}
  });

  $('file').addEventListener('change',e=>{const f=e.target.files&&e.target.files[0];if(f)vm.loadFile(f);});
  $('reset').addEventListener('click',()=>vm.reset());

  function sx(x){return(x-vm.view.x0)/(vm.view.x1-vm.view.x0)*canvas.clientWidth;}
  function sy(y){return canvas.clientHeight-(y-vm.view.y0)/(vm.view.y1-vm.view.y0)*canvas.clientHeight;}
  function tx(px){return vm.view.x0+px/canvas.clientWidth*(vm.view.x1-vm.view.x0);}
  function ty(py){return vm.view.y0+(canvas.clientHeight-py)/canvas.clientHeight*(vm.view.y1-vm.view.y0);}

  function drawCanvas(s){const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);ctx.fillStyle='#10131a';ctx.fillRect(0,0,w,h);if(!s.data){ctx.fillStyle='#8993a4';ctx.font='16px system-ui';ctx.fillText('载入 WAV 后显示主频轨迹',24,32);return;}if(s.noteLines){for(let m=12;m<128;m++){const f=440*2**((m-69)/12);if(f<s.view.y0||f>s.view.y1)continue;const y=sy(f);ctx.strokeStyle=m===60?'#3b82f6':'rgba(120,140,200,.22)';ctx.lineWidth=m===60?1.5:1;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();if(s.view.y1-s.view.y0<5000||m%12===0){ctx.fillStyle='#8c98aa';ctx.font='11px system-ui';ctx.fillText(`${NOTE[m%12]}${Math.floor(m/12)-1}`,5,y-3);}}}const line=(pts,stroke,width)=>{ctx.strokeStyle=stroke;ctx.lineWidth=width;ctx.beginPath();let st=false;for(const p of pts){if(p[0]<s.view.x0||p[0]>s.view.x1)continue;const x=sx(p[0]),y=sy(p[1]);if(!st){ctx.moveTo(x,y);st=true;}else ctx.lineTo(x,y);}if(st)ctx.stroke();};if(s.sampled)for(let i=0;i<3;i++)line(s.data.sampledTrack.map(p=>[p[0],p[1][i]]),'rgba(168,85,247,.55)',1.2);line(s.data.track,'#ff355d',s.track==='max'?2.8:1.8);const bd=60/Math.max(1,Number(s.bpm));if(s.beatLines){for(let t=Math.floor(s.view.x0/bd)*bd;t<=s.view.x1;t+=bd){const n=Math.round(t/bd),strong=n%Number(s.meter.split('/')[0])===0;ctx.strokeStyle=strong?'rgba(30,150,255,.7)':'rgba(100,150,255,.25)';ctx.lineWidth=strong?2:1;ctx.beginPath();ctx.moveTo(sx(t),0);ctx.lineTo(sx(t),h);ctx.stroke();}}if(s.beatNotes)for(const b of s.notes){if(b.t<s.view.x0-bd||b.t>s.view.x1)continue;const x=sx(b.t),rw=Math.max(30,sx(b.t+bd*.8)-x),boxY=28,dotX=x+rw,dotY=boxY-8;ctx.fillStyle=b.dismiss?'rgba(70,70,80,.35)':'#315f9e';ctx.fillRect(x,boxY,rw,34);ctx.strokeStyle=b.full?'#ffd43b':'#7da7df';ctx.strokeRect(x,boxY,rw,34);ctx.fillStyle='#fff';ctx.font='bold 13px system-ui';ctx.textAlign='center';ctx.fillText(b.dismiss?'—':b.name,x+rw/2,boxY+15);ctx.font='10px system-ui';ctx.fillText(b.dismiss?'已消除':`${b.freq.toFixed(1)}Hz`,x+rw/2,boxY+29);ctx.textAlign='left';if(s.dismiss){ctx.save();ctx.fillStyle=b.dismiss?'#f6c343':'#ef4444';ctx.beginPath();ctx.arc(dotX,dotY,7,0,Math.PI*2);ctx.fill();ctx.lineWidth=2;ctx.strokeStyle='#10131a';ctx.stroke();ctx.restore();}}if(s.playPos>0){ctx.strokeStyle='rgba(0,255,100,.9)';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(sx(s.playPos),0);ctx.lineTo(sx(s.playPos),h);ctx.stroke();}ctx.strokeStyle='#333b48';ctx.strokeRect(.5,.5,w-1,h-1);ctx.fillStyle='#8f9aaa';ctx.font='11px system-ui';for(let i=0;i<=10;i++){const t=s.view.x0+(s.view.x1-s.view.x0)*i/10;ctx.fillText(`${t.toFixed(2)}s`,sx(t)+2,h-4);}}

  canvas.addEventListener('wheel',e=>{if(!vm.data)return;e.preventDefault();const cx=tx(e.offsetX),cy=ty(e.offsetY),k=e.deltaY>0?1.12:.88;vm.view.x0=Math.max(0,cx-(cx-vm.view.x0)*k);vm.view.x1=Math.min(vm.data.duration,cx+(vm.view.x1-cx)*k);vm.view.y0=Math.max(0,cy-(cy-vm.view.y0)*k);vm.view.y1=Math.min(vm.data.fMax,cy+(vm.view.y1-cy)*k);if(vm.view.x1-vm.view.x0<.01)vm.view.x1=vm.view.x0+.01;vm.draw();},{passive:false});
  canvas.addEventListener('mousedown',e=>{drag={x:e.offsetX,y:e.offsetY,x0:vm.view.x0,x1:vm.view.x1,y0:vm.view.y0,y1:vm.view.y1};});window.addEventListener('mouseup',()=>drag=null);canvas.addEventListener('mousemove',e=>{if(!drag||!vm.data)return;const dx=(e.offsetX-drag.x)/canvas.clientWidth*(drag.x1-drag.x0),dy=(e.offsetY-drag.y)/canvas.clientHeight*(drag.y1-drag.y0);vm.view.x0=Math.max(0,drag.x0-dx);vm.view.x1=Math.min(vm.data.duration,drag.x1-dx);vm.view.y0=Math.max(0,drag.y0+dy);vm.view.y1=Math.min(vm.data.fMax,drag.y1+dy);vm.draw();});
  canvas.addEventListener('click',e=>{if(!vm.data)return;const x=tx(e.offsetX),y=ty(e.offsetY),bd=60/Math.max(1,Number(vm.bpm));if(e.ctrlKey){vm.playPos=Math.max(0,Math.min(vm.data.duration,x));if(vm.playing)startPlayback(vm,vm.playPos);vm.draw();return;}if(!vm.beatNotes)return;if(vm.dismiss){for(const b of vm.notes){if(b.t<vm.view.x0-bd||b.t>vm.view.x1)continue;const bx=sx(b.t),rw=Math.max(30,sx(b.t+bd*.8)-bx),dotX=bx+rw,dotY=28-8;if(Math.hypot(e.offsetX-dotX,e.offsetY-dotY)<=13){b.dismiss=!b.dismiss;vm.draw();return;}}}const b=vm.notes.find(n=>x>=n.t&&x<=n.t+bd*.8&&y>0&&y<vm.view.y1*.35);if(!b)return;if(e.shiftKey&&vm.dismiss){b.dismiss=!b.dismiss;vm.draw();return;}if(b.dismiss){b.dismiss=false;vm.draw();return;}const idx=b.candidates.findIndex(c=>c[0]===b.name),c=b.candidates[(idx+1)%Math.max(1,b.candidates.length)];if(c){b.name=c[0];b.freq=c[1];}else b.full=!b.full;vm.draw();});

  function readWav(bytes){const ascii=(o,n)=>String.fromCharCode(...bytes.subarray(o,o+n));if(ascii(0,4)!=='RIFF'||ascii(8,4)!=='WAVE')throw Error('不是有效的 RIFF/WAVE 文件');const dv=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);let p=12,fmt=null,ds=null;while(p+8<=bytes.length){const id=ascii(p,4),n=dv.getUint32(p+4,true);p+=8;if(id==='fmt ')fmt={format:dv.getUint16(p,true),channels:dv.getUint16(p+2,true),sampleRate:dv.getUint32(p+4,true),bits:dv.getUint16(p+14,true)};else if(id==='data'){ds=[p,Math.min(n,bytes.length-p)];break;}p+=n+(n&1);}if(!fmt||!ds)throw Error('WAV 缺少 fmt 或 data 块');if(!((fmt.format===1&&fmt.bits===16)||(fmt.format===3&&fmt.bits===32)))throw Error('仅支持 16-bit PCM 或 32-bit float WAV');const[start,size]=ds,b=fmt.bits/8,frames=Math.floor(size/b/fmt.channels),mono=new Float64Array(frames);for(let i=0;i<frames;i++){let sum=0;for(let c=0;c<fmt.channels;c++){const o=start+(i*fmt.channels+c)*b;sum+=fmt.format===1?dv.getInt16(o,true)/32768:dv.getFloat32(o,true);}mono[i]=sum/fmt.channels;}return{mono,sampleRate:fmt.sampleRate};}
  function fft(re,im){const n=re.length;for(let i=1,j=0;i<n;i++){let bit=n>>1;for(;j&bit;bit>>=1)j^=bit;j^=bit;if(i<j){[re[i],re[j]]=[re[j],re[i]];[im[i],im[j]]=[im[j],im[i]];}}for(let len=2;len<=n;len<<=1){const a=-2*Math.PI/len,co=Math.cos(a),si=Math.sin(a);for(let i=0;i<n;i+=len){let wr=1,wi=0;for(let j=0;j<len/2;j++){const u=i+j,v=u+len/2,tr=re[v]*wr-im[v]*wi,ti=re[v]*wi+im[v]*wr;re[v]=re[u]-tr;im[v]-=ti;re[u]+=tr;im[u]+=ti;const nw=wr*co-wi*si;wi=wr*si+wi*co;wr=nw;}}}}
  function nearest(f){if(!(f>0))return['—',0];const m=Math.round(69+12*Math.log2(f/440));return[`${NOTE[(m%12+12)%12]}${Math.floor(m/12)-1}`,440*2**((m-69)/12)];}
  function analyzeWav(bytes,win,hop){const{mono,sampleRate:sr}=readWav(bytes),hann=Float64Array.from({length:win},(_,i)=>.5*(1-Math.cos(2*Math.PI*i/(win-1)))),track=[],sampledTrack=[],tonesTrack=[],half=win>>1,nyq=sr/2;for(let start=0;start+win<=mono.length;start+=hop){const re=new Float64Array(win),im=new Float64Array(win);for(let i=0;i<win;i++)re[i]=mono[start+i]*hann[i];fft(re,im);const peaks=[];for(let k=1;k<half;k++)peaks.push([k,re[k]*re[k]+im[k]*im[k]]);peaks.sort((a,b)=>b[1]-a[1]);const top3=peaks.slice(0,3).map(([k])=>Math.min(nyq,k*sr/win));while(top3.length<3)top3.push(0);const seen=new Set(),top9=[];for(const[k,pow]of peaks){const f=k*sr/win,[name,nf]=nearest(f);if(!(nf>0)||seen.has(name))continue;seen.add(name);top9.push([name,nf,pow]);if(top9.length===9)break;}const t=start/sr;track.push([t,top3[0]]);sampledTrack.push([t,top3]);tonesTrack.push([t,top9]);}return{track,sampledTrack,tonesTrack,duration:mono.length/sr,sampleRate:sr,fMax:nyq};}
  function makeAudioBuffer(buf,sr){const a=audioCtx.createBuffer(1,buf.length,sr);a.copyToChannel(buf,0);return a;}
  async function startPlayback(s,offset){if(source)try{source.stop();}catch{};if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext)();await audioCtx.resume();const sr=44100,len=Math.ceil(s.data.duration*sr),out=new Float32Array(len),mode=s.track;let p=s.data.track;if(mode==='s1'||mode==='s2'||mode==='s3'){const i=Number(mode[1])-1;p=s.data.sampledTrack.map(x=>[x[0],x[1][i]]);}if(mode==='notes'){const bd=60/Math.max(1,Number(s.bpm));for(const n of s.notes){if(n.dismiss||!(n.freq>0))continue;const st=Math.floor(n.t*sr),l=Math.floor(bd*(n.full?1:.9)*sr);for(let i=0;i<l&&st+i<out.length;i++){const env=Math.min(.7,i/(sr*.01),(l-i)/(sr*.1));out[st+i]+=.3*env*Math.sin(2*Math.PI*n.freq*i/sr);}}}else{let phase=0,k=0;for(let i=0;i<len;i++){const t=i/sr;while(k+1<p.length&&t>p[k+1][0])k++;let f=k+1<p.length?p[k][1]:(p.at(-1)?.[1]||0);if(k+1<p.length){const[t0,f0]=p[k],[t1,f1]=p[k+1];f=f0+(t-t0)/(t1-t0)*(f1-f0);}phase+=2*Math.PI*Math.max(0,Math.min(sr/2,f))/sr;out[i]=.25*Math.sin(phase);}}if(mode==='original'){const ab=await audioCtx.decodeAudioData(s.fileBuffer.slice(0));source=audioCtx.createBufferSource();source.buffer=ab;}else{source=audioCtx.createBufferSource();source.buffer=makeAudioBuffer(out,sr);}source.connect(audioCtx.destination);source.onended=()=>{s.playing=false;s.playPos=0;cancelAnimationFrame(raf);s.draw();};source.start(0,offset);s.playing=true;playStart=audioCtx.currentTime-offset;cancelAnimationFrame(raf);tick(s);}
  function stopPlayback(s){if(source)try{source.stop();}catch{};s.playing=false;cancelAnimationFrame(raf);raf=0;s.playPos=0;s.draw();}
  function tick(s){if(!s.playing)return;s.playPos=Math.min(s.data.duration,audioCtx.currentTime-playStart);s.draw();raf=requestAnimationFrame(()=>tick(s));}
})();
